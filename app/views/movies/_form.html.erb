<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <strong><%= form.label :name, style: "display: block" %></strong>
    <%= form.text_field :name, class:"form-control", placeholder:"Nombre" %>
  </div>

  <div class="input-group mb-3">
    <div class="input-group-prepend">
      <%= form.label :client_id, style: "display: block", class:"input-group-text" %>
    </div>
    <%= form.select :client_id, options_for_select(@clients), class:"custom-select" %>
  </div>

  <div class="mb-3">
    <%= form.submit class:"btn btn-primary"%>
  </div>
<% end %>


